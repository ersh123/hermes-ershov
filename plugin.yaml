name: hermes-ershov
version: 0.5.0
description: >-
  Self-audit engine for Hermes operator memory. Replaces nightly memory with
  dialog-driven self-audit pipeline. Commands: create, review, nightly, diff,
  validate, apply, discard, compact, install-cron, install-systemd, status,
  soak, update.
author: Niko
kind: standalone
