name: self-ershov-memory
version: 0.5.0
description: >-
  Self-audit engine for Hermes operator memory. Dialog-driven memory
  consolidation — analyzes conversations, extracts corrections, updates
  USER.md/MEMORY.md, creates skills. Idempotent, dedup, snapshot-based.
author: Niko
kind: standalone
